name: CI
on:
  push:
    branches: [ feat/64-ci-pipelines ]
  pull_request:
    branches: [ feat/64-ci-pipelines ]
jobs:
  build:
    runs-on: readreceipt
    steps:
      - uses: actions/checkout@v6
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.x"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
      - name: Run tests
        run: |
          if [ -f setup.py ]; then python setup.py test; fi
